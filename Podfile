# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'iOS_Lab001' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for iOS_Lab001
  pod 'Then', :inhibit_warnings => true #, :binary => true
  pod 'SnapKit', '~> 4.0.0', :inhibit_warnings => true #, :binary => true

  pod 'RxSwift', '~> 5.0', :inhibit_warnings => true #, :binary => true
  pod 'RxCocoa', '~> 5.0', :inhibit_warnings => true #, :binary => true

  pod 'RxDataSources', :inhibit_warnings => true #, :binary => true

end
